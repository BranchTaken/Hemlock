(executables
 (names hocc)
 (libraries Basis Hmc))

(rule
  (deps (universe))
  (target Version.ml)
  (action
   (with-accepted-exit-codes 0
    (with-stdout-to Version.ml (run ./hocc_version)))))

(install
 (section bin)
  (files (hocc.exe as hocc)))
